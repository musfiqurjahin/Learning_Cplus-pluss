#include <iostream>

/*arithmetic operators = return with the result of
   a specifiic arithmetic operation (such as + , - , * , / )  */

int main()
{

    int students = 57;

    // students = students + 3;
    // students+=3;
    // students++;//increement
    // students = students - 1 ;
    // students-=1;
    students--; // Decreement

    std::cout << students;

    return 0;
}