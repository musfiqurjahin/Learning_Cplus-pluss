#include <iostream>

/*arithmetic operators = return with the result of
   a specifiic arithmetic operation (such as + , - , * , / )  */

int main()
{

    int students = 57;

    // students = students * 2;
    students *= 2;

    std::cout << students;

    return 0;
}