#include <iostream>

/*arithmetic operators = return with the result of
   a specifiic arithmetic operation (such as + , - , * , / )  */

int main()
{

    double students = 57;

    // students = students / 3;
    students /= 4;

    std::cout << students;

    return 0;
}